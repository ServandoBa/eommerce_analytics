version: 2

models:
  - name: fct_orders
    description: Fact table de ordenes agregadas a nivel order_id.
    columns:
      - name: order_id
        tests:
          - not_null
          - unique
      - name: customer_id
        tests:
          - not_null
